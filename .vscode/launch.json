{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/my-husky-project/package.json",
                "localRoot": "${workspaceFolder}/my-husky-project"
            }
        }
    ]
}